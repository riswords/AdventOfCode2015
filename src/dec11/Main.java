package dec11;

public class Main {

    /**
     * Well, after a brute force attempt and a slightly more optimized attempt that both resulted 
     * in stack overflows, I decided to just think about the answer. No code was actually used 
     * to solve this one. Hours of coding can save you minutes of thinking. Or you can just do the 
     * thinking...Which one is actually lazier?
     */
    public static void main(String[] args) {
        if(args.length > 0) {
            //BruteForcePasswordGenerator generator = new BruteForcePasswordGenerator();
            //AbstractPasswordGenerator generator = new MemoizedPasswordGenerator();
            //System.out.println("Next password: " + generator.getNextPassword(args[0]));
        }
    }

}
